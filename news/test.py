from news.pipelines import NewsDownloadPipeline
newname = [str(x) + '.jpg' for x in range(1, 100)]
print(newname)
for i in range(0, len(image_paths)):
	print(len())
    os.renames("C:\\anker\\" + image_paths[i], "C:\\anker\\full/" + newname[i])
